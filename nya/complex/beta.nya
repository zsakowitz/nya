use "real/parity";
use "gamma/lngamma";

fn sign_beta(x: num, y: num) -> num {
  sign_gamma(x) * sign_gamma(y) * sign_gamma(x + y)
}

fn ln_beta(x: num, y: num) -> num {
  lngamma(x) + lngamma(y) - lngamma(x + y)
}

fn beta(x: num, y: num) -> num {
  sign_beta(x, y) * exp(ln_beta(x, y))
}
